*secrets.json
logs.txt
__pycache__
secrets.json
*chains.json
chains.json
errors.json
.history
.govbot.lock
.postscan.lock
cosmos-governance-bot.log
cosmos-posts-scan-bot.log

# The globs above anchor on the end of the name, so the copies operators leave behind
# (secrets.json.bak, secrets.json.save, ...) match nothing and stage live tokens.
secrets.json.*
chains.json.*
*.bak
*.backup
*.save
*.orig
*~
*.swp

